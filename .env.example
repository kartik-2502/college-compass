# Database
DATABASE_URL="file:./dev.db"

# NextAuth — generate with: openssl rand -base64 32
AUTH_SECRET="college-compass-dev-secret-change-in-production"

# App URL (used in production deployments)
NEXTAUTH_URL="http://localhost:3000"
